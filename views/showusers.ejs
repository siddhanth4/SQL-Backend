<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOW USERS</title>
    <style>
        table, tr, th, td{
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>UserName</th>
            <th>Email</th>
            <th>Password</th>
        </tr>
        <% for(user of users){ %>
            <tr>
                <td><%= user.username %></td>
                <td><%= user.email %></td>
                <td> <%= user.password %></td>
                <td><form method="GET" action="/user/<%= user.username %>/edit">
                    <button>Edit usernaame</button>
                </form>
                </td>
            </tr>
        <% } %>
    </table>
</body>
</html>
